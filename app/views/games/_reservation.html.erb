<%= form_for @game do |f| %>
  <%= f.fields_for :reservations, Reservation.new do |reservation_fields| %>
    <%= reservation_fields.hidden_field :player_id, value: current_user.id %>
    <%= reservation_fields.hidden_field :game_id, value: @game.id %>
  <% end %>
<% end %>